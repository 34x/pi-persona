# Example: linked persona + multiline inline context
# Usage: /persona strict-dev
name: Strict Dev
description: Development expert with strict TypeScript enforcement
persona: tdd-expert.md
context:
  - coding-guidelines.md
  - |
    Always use TypeScript strict mode.
    Follow the backend architecture patterns.
    Never use the `any` type.